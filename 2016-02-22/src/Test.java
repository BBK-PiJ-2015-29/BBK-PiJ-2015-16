public class Test {
    private static final int MAGIC = 100;
    private Vital obj;

    public boolean add(int x) {
//        if (x > MAGIC){
//            return true;
//        } else {
//            return false;
//        }
        boolean ans = true;
        //...

        if (ans == true) {
            //...
        }
        return x > MAGIC;
        // if (x > 0)
        //    for(...)
        // asdahsjd

        // push(1)
        // push(2)
        // ..
        // push(10)

        // run test
        // do something
        // next bit

        // run test
        // do something
        // next bit

        // run test
        // do something
        // next bit

        // run test
        // do something
        // next bit

    }

    public Vital meth() {
        return obj;
    }

    public String myString() {
        String s = null;


        s += "thing"; // inefficient - prefer StringBuilder instead
        return s;
    }


}

interface Vital {
}
